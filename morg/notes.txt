m68k-elf-gcc -Wall -g src/main.c -c -m68000 -o main.o
m68k-elf-ld -o morg.S68 -T linker.ld main.o








- make .bin file
- convert that to iso file
- run qemu using the iso file
- run gdb with 'target remote localhost:1234' to debug the code


dd if=/dev/zero of=floppy.img bs=1024 count=1440
dd if=boot.bin of=floppy.img seek=0 count=1 conv=notrunc

mkdir iso
cp floppy.img iso/
genisoimage -quiet -V 'MYOS' -input-charset iso8859-1 -o myos.iso -b floppy.img \
    -hide floppy.img iso/